# PENDING ROLLOUT ONLY. Do not append to an active deployment automatically.
# User selected 3/5/8/10/20 dynamic ceilings on 2026-09-12.
# Apply only after verified flat/no-pending/no-uncertain Demo exposure, a drained
# scheduler, accepted safety fixes and configuration validation. Never close or
# change existing cross-margin positions merely to satisfy this prerequisite.
# Preserve existing credentials, execution controls, protection and risk limits.
# This is not a standalone .env. The reviewed base already requires score risk,
# capital buckets, continuous session and protection enabled, with cooldown 0;
# validate those prerequisites without enabling unrelated controls here.
# Restart disarms; the existing arm path still requires exchange and local flat.
# This overlay cannot arm, start, enable writes or enable Live trading.
OKX_DEMO_MAX_LEVERAGE=20
OKX_DEMO_STRUCTURAL_DYNAMIC_LEVERAGE_ENABLED=true
OKX_DEMO_STRUCTURAL_LOW_LEVERAGE_CAP=3
OKX_DEMO_STRUCTURAL_MEDIUM_LEVERAGE_CAP=5
OKX_DEMO_STRUCTURAL_HIGH_LEVERAGE_CAP=8
OKX_DEMO_STRUCTURAL_ELITE_LEVERAGE_CAP=10
OKX_DEMO_STRUCTURAL_EXTREME_LEVERAGE_CAP=20
OKX_DEMO_STRUCTURAL_LOW_RISK_PCT=0.005
OKX_DEMO_STRUCTURAL_MEDIUM_RISK_PCT=0.005
OKX_DEMO_STRUCTURAL_HIGH_RISK_PCT=0.005
OKX_DEMO_STRUCTURAL_ELITE_RISK_PCT=0.005
OKX_DEMO_STRUCTURAL_EXTREME_RISK_PCT=0.005
OKX_DEMO_PORTFOLIO_MAX_RISK_PCT=0.01
